extends layout 

block content 
    .w3-card-4
        header.w3-container.w3-red.w3-margin-bottom.w3-p
            h1= title
        .w3-container 
            ul 
                li= student.nome 
                li= student.git 
            .w3-container.w3-margin-8 
                p TPC's Concluídos: 
                ul
                    - for (var i = 1; i <= 8; i++)              
                        - var tpc = 'tpc' + i     
                        - if (student[tpc] === true)
                            li TPC#{i}   
                                  
                        
